export enum ChallengeStatus {
    EXECUTED = 'EXECUTED',
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    DENIED = 'DENIED',
    CANCELED = 'CANCELED',
}

export enum UpdateChallengeStatus {
    ACCEPTED = 'ACCEPTED',
    DENIED = 'DENIED',
    CANCELED = 'CANCELED',
}
